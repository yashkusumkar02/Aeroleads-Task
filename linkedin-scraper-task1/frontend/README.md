Frontend (Vite + React + TS)

Run
```
npm install
npm run dev
# open http://localhost:5173
```

Notes
- The dev server proxies /api and /images to http://localhost:4000 (see vite.config.ts).
- Ensure the backend is running and the scraper has produced profiles.csv and images/.




